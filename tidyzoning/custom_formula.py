def custom_formula(x, constant=5):
    """
    Calculate the result of the formula: x^2 + 3x - constant.

    Args:
        x (float): The input number.
        constant (float): A constant to subtract (default is 5).

    Returns:
        float: The result of the formula.
    """
    return 2*x - constant